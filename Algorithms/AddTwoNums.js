// write a function that takes in two numbers and adds the numbers together;

// Example Returns func(5,6) // 11 func(10,0) //10

function addTwoNums(numA, numB) {
	return numA + numB;
}

//Or

const addTwoNums2 = (numA, numB) => numA + numB;
console.log(addTwoNums(2, 5));
addTwoNums(2, 5);
